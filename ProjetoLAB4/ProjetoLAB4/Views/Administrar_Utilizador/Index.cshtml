@model IEnumerable<ProjetoLAB4.Models.Administrar_Utilizador>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID_Administrador)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ID_Utilizadorr)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Motivo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.data_inicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.data_fim)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID_Administrador)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ID_Utilizadorr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Motivo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.data_inicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.data_fim)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Adminstrar_Utilizador">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Adminstrar_Utilizador">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Adminstrar_Utilizador">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
